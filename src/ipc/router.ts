import { app } from "./app";
import { auth } from "./auth";
import { shell } from "./shell";
import { theme } from "./theme";
import { window } from "./window";

export const router = {
  auth,
  theme,
  window,
  app,
  shell,
};
